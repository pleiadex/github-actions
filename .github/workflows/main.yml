name: String contain assertion test
on:
  pull_request:
  push:
jobs:
  test:
    name: string contain test
    runs-on: ubuntu-latest
    container:
      image: hub.docker.com/pleiadex/circom
    steps:
    - uses: actions/checkout@v3
#     - uses: GuillaumeFalourd/assert-command-line-output@v2.1
#       with:
#         command_line: cat result.txt
#         assert_file_path: result.txt
#         expected_result: FAILED
    - run : circom --help 
    - run : echo "VALID" > output.txt

    - uses: GuillaumeFalourd/diff-action@v1
      with:
        first_file_path: output.txt
        second_file_path: result.txt
        expected_result: PASSED
